$('#project_list').prepend('<%=escape_javascript render :partial => 'project_box', :locals => {:project => @project} %>');
ajax_flash_notice('New Project created successfully!!');
$('#<%=@project.id%>_project_box').effect("highlight", {color: "#ffd900"}, 2000);

