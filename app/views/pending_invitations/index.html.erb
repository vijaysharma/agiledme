<h1>Listing pending_invitations</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Project</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pending_invitations.each do |pending_invitation| %>
  <tr>
    <td><%= pending_invitation.email %></td>
    <td><%= pending_invitation.project_id %></td>
    <td><%= link_to 'Show', pending_invitation %></td>
    <td><%= link_to 'Edit', edit_pending_invitation_path(pending_invitation) %></td>
    <td><%= link_to 'Destroy', pending_invitation, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pending invitation', new_pending_invitation_path %>
