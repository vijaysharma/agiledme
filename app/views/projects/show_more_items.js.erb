<% if @workable_items_to_append.present? %>
<% @workable_items_to_append.each do |workable_item| %>
$('#<%= "#{@category}" %>_item_list_end').before('<%=escape_javascript render :partial => 'workable_items/workable_item', :locals => {:workable_item => workable_item} %>');
attachTokenInput($('#<%= workable_item.id %>_labels'));
<% end %>
<% end %>