$('#workable_item_panels').append('<%=escape_javascript render :partial => 'projects/workable_items_panel', :locals => {:workable_items => @search_results, :category => "search", :search_term => @search_term } %>');
<% if @search_results.present? %>
<% @search_results.each do |workable_item| %>
attachTokenInput($('#<%= workable_item.id %>_labels'));
<% end %>
<% end %>
